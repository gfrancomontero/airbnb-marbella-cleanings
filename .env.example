# Airbnb hosting calendar export URLs (private — rotate if leaked).
# Listing 1: prefer AIRBNB_ICAL_URL_1; legacy single-var AIRBNB_ICAL_URL still works as apt 1.
AIRBNB_ICAL_URL_1=
AIRBNB_ICAL_LABEL_1=Apartamento 1

AIRBNB_ICAL_URL_2=
AIRBNB_ICAL_LABEL_2=Apartamento 2

# Legacy (optional): treated as first listing if AIRBNB_ICAL_URL_1 is unset
# AIRBNB_ICAL_URL=
# AIRBNB_ICAL_LABEL=
